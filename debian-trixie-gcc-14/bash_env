export LAB=/home/$INAME

path_add() {
  case ":$PATH:" in
    *":$1:"*) ;;
    *) PATH="$1:$PATH" ;;
  esac
}

path_add /root/.local/bin
path_add "$LAB"
path_add "$LAB/bin"

export PATH

export LD_LIBRARY_PATH=/usr/local/lib
