# Tests {#tests}

## Run quality tests
open a terminal & run the following command:
cd in the root folder and run the following command:

```
./local-ci.sh

```

## Run the coverage tests
open a terminal & run the following command:
cd in the root folder and run the following command:

```
./local-ci.sh

```

## Run unit tests
run the following commands:

```
./local-ci.sh

```

## Consult the reports & documentation
run the following commands:

```
./local-ci.sh

```
and view the reports & documentation in the reports folder.
